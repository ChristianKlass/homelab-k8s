apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: sops-secrets-operator-system
  namespace: flux-system
spec:
  interval: 10m
  path: ./sops-secrets-operator-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
