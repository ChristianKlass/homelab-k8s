apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gitlab-agent
  namespace: flux-system
spec:
  interval: 10m
  path: ./gitlab-agent
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
